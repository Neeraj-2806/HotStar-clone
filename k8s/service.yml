apiVersion: v1
kind: Service
metadata:
  name: hotstar-service
  namespace: hotstar
spec:
  type: LoadBalancer        # change to ClusterIP / LoadBalancer if needed
  selector:
    app: hotstar
  ports:
    - protocol: TCP
      port: 80          # service port
      targetPort: 3000  # container port
